import {Image} from 'react-native';
import styled from 'styled-components/native';

export const ImageWrapper = styled(Image)`
  width: 250px;
  height: 250px;
`;

export const ImageAssessWrapper = styled(Image)`
  width: 100px;
  height: 100px;
`;
